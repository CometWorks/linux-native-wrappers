// Included relative to the root file's directory; includes its sibling with
// mismatched case to exercise case-insensitive resolution.
#include "nested.HLSLI"
#define LOCAL_VALUE float4(0.25, 0, 0, 0)
